{
  "name": "cc-textparser",
  "version": "1.0.1",
  "description": "A reimplementation of CrossCode's text parser",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "test": "tsx tests/index.ts",
    "build": "rimraf dist && tsc",
    "publish": "pnpm run build && npm publish"
  },
  "keywords": [
    "crosscode",
    "formatting",
    "parser"
  ],
  "author": "redstonekasi",
  "license": "BSD-3-Clause",
  "repository": "github:redstonekasi/cc-textparser",
  "devDependencies": {
    "manten": "^0.5.0",
    "rimraf": "^3.0.2",
    "tsx": "^3.11.0",
    "typescript": "^4.8.4"
  }
}
